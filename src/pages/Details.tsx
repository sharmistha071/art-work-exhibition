import { useState } from "react";
import "./App.css";

const Details = () => {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>hello</p>
    </div>
  );
};

export default Details;
